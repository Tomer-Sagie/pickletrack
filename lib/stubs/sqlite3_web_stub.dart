/// Web stub — [sqlite3_flutter_libs] uses `dart:ffi` which is not available
/// on the web platform. This stub provides the same public API surface so
/// the conditional import in `main.dart` compiles on both native and web.
Future<void> applyWorkaroundToOpenSqlite3OnOldAndroidVersions() async {}
